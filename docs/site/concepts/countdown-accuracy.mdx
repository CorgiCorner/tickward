---
title: "Countdown Accuracy"
description: "How tickward computes remaining time, which browser APIs it relies on, and where to check their support."
---

Every timer stores its target as an absolute UTC timestamp. The countdown you
see is always the difference between that target and the current time, computed
fresh on every tick - never a stored counter that gets decremented.

## How remaining time is computed

When you create a timer, the date, time, and time zone you pick are converted
to a UTC ISO 8601 timestamp once, at save time. From then on the remaining time
is pure arithmetic, implemented by `getCountdownParts` in `lib/utils.ts`:

```ts
const targetMs = new Date(targetDateIsoUtc).getTime()
const deltaMs = targetMs - nowMs
const isCountUp = deltaMs < 0
const totalMs = Math.abs(deltaMs)

const totalSeconds = Math.floor(totalMs / 1000)
const days = Math.floor(totalSeconds / 86400)
const hours = Math.floor((totalSeconds % 86400) / 3600)
const minutes = Math.floor((totalSeconds % 3600) / 60)
const seconds = totalSeconds % 60
```

The delta is split into days, hours, minutes, and seconds with integer
division. Days are total days, not calendar months or weeks. When a one-off
target has passed, the same numbers are shown as a count-up ("since") and the
timer can enter [Started counting up](/concepts/started-counting-up) for review.
Repeating timers advance to their next occurrence instead.

## Where "now" comes from

The current time is sampled by a single shared ticker in
`lib/now-ticker.client.ts`. It runs a dedicated Web Worker that posts a message
once per second; on every message the main thread reads `Date.now()` and
notifies every subscribed component. Components subscribe through the `useNow`
hook in `components/use-now.ts` (a thin `useSyncExternalStore` wrapper), so the
whole app shares one tick source. When Web Workers are unavailable, the ticker
falls back to a plain `setInterval`.

The worker matters for background tabs: browsers heavily throttle main-thread
timers in hidden tabs, but timers inside dedicated workers keep firing, so the
countdown display stays live while you are on another tab. (Alarms do not depend
on this tick reaching the boundary on time - they are scheduled at the absolute
target instead; see
[Notifications & Alarms](/concepts/notifications-and-alarms).) The ticker also
re-samples immediately when the tab becomes visible again (`visibilitychange`),
so a tab that was frozen entirely shows the correct time the moment you return.

Two practical consequences:

- **No drift.** Because each tick recomputes `target - now` from absolute
  timestamps, a delayed tick cannot accumulate error. If the tab was frozen or
  the laptop slept, the display may pause, but the very next tick is exactly
  right again.
- **It trusts the device clock.** `Date.now()` is the device's wall clock.
  If the device clock is two minutes off, the displayed remaining time is two
  minutes off as well. There is no server time synchronization.

## Time zones and recurrence

Converting your wall-clock input ("June 30 at 09:00 in Europe/Warsaw") to UTC
uses the `date-fns-tz` library, which delegates to the browser's built-in
`Intl.DateTimeFormat` and its IANA time zone database. Daylight saving rules
come from the browser, so they stay current with browser updates.

Recurring timers store a wall-clock pattern in the timer's time zone and
compute the next occurrence on demand. Occurrences are calculated, never
mutated, so the countdown is correct even if the app was closed while several
occurrences passed. They never create count-up review occurrences.

## Browser support

| API | Used for | Support |
|-|-|-|
| `Date.now()` | sampling the current time | every browser since ES5 |
| `Worker` (dedicated Web Worker) | the 1-second tick that survives background-tab throttling | all evergreen browsers |
| `setInterval` | tick fallback when workers are unavailable | universal |
| Page Visibility API (`visibilitychange`) | instant re-sample when the tab becomes visible | all evergreen browsers |
| `Intl.DateTimeFormat` with IANA `timeZone` | time zone conversion | all evergreen browsers |

All of these APIs are available in every current evergreen browser, and the
ticker degrades to `setInterval` where workers are missing; the only API with a
meaningful history of partial support is `Intl.DateTimeFormat` time zone
handling in long-obsolete browsers.

To verify support yourself:

- [caniuse.com](https://caniuse.com) - search the feature, for example
  [Internationalization API](https://caniuse.com/internationalization) for
  `Intl.DateTimeFormat` or
  [Date.now](https://caniuse.com/mdn-javascript_builtins_date_now).
- [MDN](https://developer.mozilla.org) - every API page ends with a browser
  compatibility table, for example
  [Intl.DateTimeFormat](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/DateTimeFormat#browser_compatibility).
